<h1>Edit User</h1>
<%= simple_form_for @user, :html => {:multipart => true} do |f| %>
	<%= f.input :name, label: "Name" %>
 	<%= f.input :address, label: "Address" %>
 	<%= f.input :telephone, label: "Telephone Number" %>
  	<%= f.button :submit %>
<% end %>

<%= link_to 'Back to User List', users_path %>